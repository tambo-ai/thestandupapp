---
phase: 06-team-scoped-ai-tools
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/tambo.ts
autonomous: true
requirements: [AI-03, AI-04]
gap_closure: true

must_haves:
  truths:
    - "AI presents partial PR results for connected members when some members lack GitHub"
    - "AI notes which members had errors without treating the entire query as failed"
    - "AI presents partial issue results for connected members when some members lack Linear"
  artifacts:
    - path: "src/lib/tambo.ts"
      provides: "Updated tool descriptions and outputSchema with partial-success guidance"
  key_links:
    - from: "src/lib/tambo.ts (getPullRequests description)"
      to: "AI behavior"
      via: "tool description instructs AI on partial error handling"
      pattern: "partial.*results|errors.*does not mean"
    - from: "src/lib/tambo.ts (outputSchema errors field)"
      to: "AI interpretation"
      via: ".describe() on errors field"
      pattern: "errors.*describe"
---

<objective>
Fix AI handling of partial failures in team-scope queries so it shows results for connected members and notes errors for disconnected ones, instead of treating the whole response as a failure.

Purpose: UAT test 7 failed — "show me everyone's PRs" returned "GitHub returned Unauthorized" blanket error instead of partial results. The root cause is that the tool description and outputSchema errors field lack guidance for the AI on how to handle partial success. The code itself (Promise.allSettled, error arrays) works correctly — the AI model just needs better instructions.

Output: Updated tool descriptions and output schemas for getPullRequests and searchIssues
</objective>

<execution_context>
@/Users/avi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/avi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-team-scoped-ai-tools/06-03-SUMMARY.md
@.planning/debug/team-scope-pr-unauthorized.md

<interfaces>
<!-- Current getPullRequests tool (src/lib/tambo.ts lines 306-387) -->

description: "Fetch GitHub pull requests for the current user or across team members. Use scope='team' to get PRs from all connected team members (results include memberName attribution). The team_roster context helper tells you which members have GitHub connected — only pass those as memberIds. Personal scope (default): fetches the current user's PRs. Supports filtering by author, repo, org, date range."

outputSchema errors field (line 381-385):
  errors: z.array(z.object({
    memberId: z.string(),
    memberName: z.string(),
    error: z.string(),
  })).optional()
  // NOTE: no .describe() — AI has no guidance on partial-success semantics

<!-- Current searchIssues tool (same pattern, lines 298-303) -->
Same errors field structure, same missing .describe()
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add partial-success guidance to tool descriptions and outputSchema errors fields</name>
  <files>src/lib/tambo.ts</files>
  <action>
In `src/lib/tambo.ts`, make these targeted changes:

1. **getPullRequests tool description** (line 308-309): Append to the existing description:
   " If the response contains both pullRequests and errors, present the successful results to the user and mention which members had errors (e.g. 'GitHub not connected for [name]'). Do NOT treat partial errors as a complete failure."

2. **getPullRequests outputSchema errors field** (lines 381-385): Add `.describe()`:
   ```
   errors: z.array(z.object({
     memberId: z.string(),
     memberName: z.string(),
     error: z.string(),
   })).optional().describe("Per-member errors (e.g. account not connected). Presence of errors does NOT mean the tool call failed — always present pullRequests results for successful members and note which members had errors.")
   ```

3. **searchIssues tool description** (around line 236): Apply the same pattern — append:
   " If the response contains both issues and errors, present the successful results and note which members had errors. Do NOT treat partial errors as a complete failure."

4. **searchIssues outputSchema errors field** (lines 298-303): Add `.describe()`:
   ```
   errors: z.array(z.object({
     memberId: z.string(),
     memberName: z.string(),
     error: z.string(),
   })).optional().describe("Per-member errors (e.g. account not connected). Presence of errors does NOT mean the tool call failed — always present issues results for successful members and note which members had errors.")
   ```

These are description-only changes. No logic, no schema shape changes. The AI model reads these descriptions to decide how to present results.
  </action>
  <verify>
    <automated>cd /Users/avi/.superset/worktrees/thestandupapp/avi/frigate && npm run build</automated>
  </verify>
  <done>
    - getPullRequests and searchIssues tool descriptions include partial-error handling instructions
    - Both outputSchema errors fields have .describe() with partial-success guidance
    - Build passes
  </done>
</task>

</tasks>

<verification>
- `npm run build` passes
- `grep -c "does NOT mean" src/lib/tambo.ts` returns at least 2 (one per tool)
- `grep -c "\.describe(" src/lib/tambo.ts` shows the new describe calls on errors fields
</verification>

<success_criteria>
Both getPullRequests and searchIssues tools have explicit AI guidance for handling partial errors gracefully. The AI model will now be instructed to present successful results alongside error notes, not treat any error as total failure.
</success_criteria>

<output>
After completion, create `.planning/phases/06-team-scoped-ai-tools/06-05-SUMMARY.md`
</output>
