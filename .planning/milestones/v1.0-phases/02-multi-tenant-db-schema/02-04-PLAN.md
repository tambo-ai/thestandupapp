---
phase: 02-multi-tenant-db-schema
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/migrate.ts
  - scripts/verify-schema.ts
  - package.json
autonomous: true
requirements: [DATA-01]
gap_closure: true

must_haves:
  truths:
    - "Running `npx tsx scripts/migrate.ts` succeeds without --env-file flag"
    - "Running `npx tsx scripts/verify-schema.ts` succeeds without --env-file flag"
    - "npm run db:migrate and npm run db:verify work as shorthand commands"
  artifacts:
    - path: "scripts/migrate.ts"
      provides: "Self-loading env config for migration runner"
      contains: "process.loadEnvFile"
    - path: "scripts/verify-schema.ts"
      provides: "Self-loading env config for schema verifier"
      contains: "process.loadEnvFile"
    - path: "package.json"
      provides: "Convenience npm scripts"
      contains: "db:migrate"
  key_links:
    - from: "scripts/migrate.ts"
      to: ".env.local"
      via: "process.loadEnvFile('.env.local')"
      pattern: "process\\.loadEnvFile"
    - from: "scripts/verify-schema.ts"
      to: ".env.local"
      via: "process.loadEnvFile('.env.local')"
      pattern: "process\\.loadEnvFile"
---

<objective>
Fix UAT gap: scripts/migrate.ts and scripts/verify-schema.ts fail when run without the `--env-file=.env.local` flag because tsx does not auto-load .env.local like Next.js does.

Purpose: Scripts should work with a plain `npx tsx scripts/migrate.ts` invocation, matching user expectations from the UAT cold start smoke test.
Output: Both scripts self-load .env.local using Node 22's built-in `process.loadEnvFile()`. Convenience npm scripts added.
</objective>

<execution_context>
@/Users/avi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/avi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-multi-tenant-db-schema/02-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add env self-loading to scripts and npm convenience commands</name>
  <files>scripts/migrate.ts, scripts/verify-schema.ts, package.json</files>
  <action>
    In both `scripts/migrate.ts` and `scripts/verify-schema.ts`, add the following block at the very top of the file (before all other imports):

    ```typescript
    // Load .env.local for standalone script execution (Next.js does this automatically, tsx does not)
    try {
      process.loadEnvFile('.env.local');
    } catch {
      // Ignore — env vars may already be set (e.g., CI, --env-file flag)
    }
    ```

    This uses Node 22's built-in `process.loadEnvFile()` — no new dependency needed. The try/catch ensures the script still works in CI where .env.local doesn't exist but env vars are set directly.

    In `package.json`, add two scripts to the "scripts" object:
    - `"db:migrate": "npx tsx scripts/migrate.ts"`
    - `"db:verify": "npx tsx scripts/verify-schema.ts"`

    Place them after the existing `"lint:fix"` entry.

    Do NOT change any other logic in the scripts or any other package.json fields.
  </action>
  <verify>
    <automated>npx tsx scripts/verify-schema.ts</automated>
  </verify>
  <done>Both scripts run successfully with plain `npx tsx scripts/X.ts` (no --env-file flag needed). `npm run db:migrate` and `npm run db:verify` work as aliases. SCHEMA VERIFICATION PASSED output confirms env vars loaded correctly.</done>
</task>

</tasks>

<verification>
1. `npx tsx scripts/migrate.ts` completes without "Please specify either client or url" error
2. `npx tsx scripts/verify-schema.ts` outputs "SCHEMA VERIFICATION PASSED"
3. `npm run db:verify` produces the same output
4. No new dependencies added to package.json
</verification>

<success_criteria>
UAT Test 1 (Cold Start Smoke Test) passes: `npx tsx scripts/migrate.ts` works without the `--env-file=.env.local` workaround.
</success_criteria>

<output>
After completion, create `.planning/phases/02-multi-tenant-db-schema/02-04-SUMMARY.md`
</output>
