---
phase: 06-team-scoped-ai-tools
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/tambo/threads/route.ts
  - src/components/tambo/thread-history.tsx
  - src/app/app/app-shell.tsx
autonomous: true
requirements: [AI-06]
gap_closure: true

must_haves:
  truths:
    - "Switching teams shows only threads created in that team context"
    - "New threads are created with composite userKey so they belong to the active team"
    - "Personal workspace threads are separate from team threads"
  artifacts:
    - path: "src/app/api/tambo/threads/route.ts"
      provides: "Server-side thread pre-creation with composite userKey"
      exports: ["POST"]
    - path: "src/components/tambo/thread-history.tsx"
      provides: "Thread list filtered by composite userKey"
    - path: "src/app/app/app-shell.tsx"
      provides: "userKey prop passed to ThreadHistory, new thread creation via API"
  key_links:
    - from: "src/components/tambo/thread-history.tsx"
      to: "useTamboThreadList"
      via: "listOptions.userKey parameter"
      pattern: "useTamboThreadList\\(\\{\\s*userKey"
    - from: "src/app/app/app-shell.tsx"
      to: "/api/tambo/threads"
      via: "fetch POST for thread pre-creation"
      pattern: "fetch.*api/tambo/threads"
---

<objective>
Fix per-team thread isolation so switching teams shows different thread history.

Purpose: UAT test 1 failed — threads are not isolated per team. The root cause is that userKey was removed from TamboProvider (due to auth conflict), so threads are created without a userKey and useTamboThreadList() returns all threads unfiltered. The fix uses the SDK's independent userKey support on thread list/create operations without touching TamboProvider auth.

Output: Server-side thread pre-creation route, filtered thread listing, team-scoped thread creation
</objective>

<execution_context>
@/Users/avi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/avi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-team-scoped-ai-tools/06-02-SUMMARY.md
@.planning/debug/team-thread-isolation.md

<interfaces>
<!-- Tambo SDK thread types (from node_modules/@tambo-ai/typescript-sdk) -->

ThreadCreateParams: { userKey?: string; metadata?: unknown; initialMessages?: [...] }
ThreadListParams: { cursor?: string; limit?: number; userKey?: string }
ThreadCreateResponse: { id: string; userKey?: string; ... }

useTamboThreadList(listOptions?: ThreadListParams, queryOptions?): { data, isLoading, error, refetch }
  -- listOptions.userKey filters threads server-side (independent of TamboProvider userKey)

useTambo(): { client, switchThread, startNewThread, initThread, currentThreadId, updateThreadName, ... }
  -- initThread(threadId: string, initialThread?: Partial<TamboThread>) => void
  -- client.threads.create(params: ThreadCreateParams) => Promise<ThreadCreateResponse>

<!-- Current app-shell.tsx TamboProvider usage (no userKey) -->
<TamboProvider apiKey={...} components={...} tools={...} userToken={userToken} contextHelpers={...} initialMessages={[systemPrompt]}>

<!-- Current thread-history.tsx -->
useTamboThreadList() // called with NO args - returns all threads
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create server-side thread pre-creation API and wire filtered thread list</name>
  <files>src/app/api/tambo/threads/route.ts, src/components/tambo/thread-history.tsx, src/app/app/app-shell.tsx</files>
  <action>
1. Create `src/app/api/tambo/threads/route.ts` — a POST endpoint that:
   - Reads `{ teamId }` from the JSON body (optional — omit for personal workspace)
   - Gets the authenticated user via `authkit()` (import from `@workos-inc/authkit-nextjs`)
   - Constructs composite userKey: if teamId is provided use `${userId}:${teamId}`, otherwise use `${userId}`
   - Creates a Tambo thread via the REST API: `POST https://api.tambo.co/v1/threads` (or use `NEXT_PUBLIC_TAMBO_URL` env if set, appending `/v1/threads`) with headers `Authorization: Bearer ${NEXT_PUBLIC_TAMBO_API_KEY}` and body `{ userKey }`. Use `process.env.NEXT_PUBLIC_TAMBO_API_KEY` for the API key.
   - Returns the created thread's `{ id, userKey }` as JSON

2. Modify `src/components/tambo/thread-history.tsx`:
   - Add a `userKey?: string` prop to `ThreadHistoryProps`
   - Pass it through to `useTamboThreadList({ userKey })` on line 82 (replacing the no-args call)
   - Also add `userKey` to the context value interface and provider so child components can access it
   - In `ThreadHistoryNewButton`, instead of calling `startNewThread()` directly, call the `onCreateThread` callback from context (added next)
   - Add `onCreateThread?: () => Promise<string | void>` to `ThreadHistoryContextValue` and pass through from props

3. Modify `src/app/app/app-shell.tsx`:
   - Compute `tamboUserKey` using the same composite pattern: `activeTeamId && !activeTeam?.isPersonal ? \`${userId}:${activeTeamId}\` : userId`
   - Create a `handleCreateThread` async function that: (a) POSTs to `/api/tambo/threads` with `{ teamId: activeTeam?.isPersonal ? undefined : activeTeamId }`, (b) reads the response `{ id }`, (c) calls `initThread(id)` from useTambo, (d) returns the id
   - Pass `userKey={tamboUserKey}` and `onCreateThread={handleCreateThread}` to the ThreadHistory component (it's used inside MessageThreadFull — check if MessageThreadFull passes props through, if not pass directly)
   - Since MessageThreadFull composes ThreadHistory internally, the cleanest approach is to lift the userKey into a React context or pass it as a prop on MessageThreadFull. Check MessageThreadFull's props — if it spreads remaining props to ThreadHistory, just add it. Otherwise, add `userKey` and `onCreateThread` props to MessageThreadFull and forward them to ThreadHistory.

IMPORTANT: Do NOT pass userKey to TamboProvider — this causes auth conflicts (the SDK returns status: "invalid" when both userKey and userToken are set). The userKey is ONLY used on useTamboThreadList() and the server-side thread creation route.

KEY INSIGHT from debug session: useTamboThreadList({ userKey }) is independent of TamboProvider-level userKey. This is the correct filtering mechanism.
  </action>
  <verify>
    <automated>cd /Users/avi/.superset/worktrees/thestandupapp/avi/frigate && npm run build</automated>
  </verify>
  <done>
    - Server-side thread creation route exists at /api/tambo/threads and accepts POST with teamId
    - useTamboThreadList receives userKey filter for team-scoped listing
    - New thread button creates threads via server route with composite userKey
    - Build passes with no type errors
  </done>
</task>

</tasks>

<verification>
- `npm run build` passes
- `/api/tambo/threads` route file exists with POST handler
- `useTamboThreadList` is called with `{ userKey }` argument (not empty)
- No `userKey` prop on TamboProvider (would break auth)
</verification>

<success_criteria>
Thread list is filtered by composite userKey (userId:teamId for teams, userId for personal). New threads are pre-created server-side with the correct userKey. Build compiles without errors.
</success_criteria>

<output>
After completion, create `.planning/phases/06-team-scoped-ai-tools/06-04-SUMMARY.md`
</output>
